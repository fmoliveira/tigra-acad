@model List<string>
@if (Model.Count() == 0) {
    @:Nenhum requisito implementado.
}
else
{
    <ul>
        @foreach (var m in Model)
        {
            <li>@Html.Raw(m)</li>
        }
    </ul>
}